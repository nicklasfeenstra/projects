task buildApplication(type: GradleBuild) {
    group 'application'
    setTasks([':server:bootJar'])
}

task buildClient(type: GradleBuild) {
	group 'application'
    setTasks([':client:build'])
}

task serveClient(type: GradleBuild) {
	group 'application'
    setTasks([':client:serve'])
}